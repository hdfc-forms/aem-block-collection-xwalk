{
  "definitions": [
    {
      "title": "Button",
      "id": "nav-button",
      "plugins": {
        "xwalk": {
          "page": {
            "resourceType": "core/franklin/components/block/v1/block",
            "template": {
              "name": "Button",
              "model": "nav-button"
            }
          }
        }
      }
    }
  ],
  "models": [
    {
      "id": "nav-button",
      "fields": [
        {
          "component": "select",
          "name": "navMode",
          "label": "Navigation Type",
          "valueType": "string",
          "required": true,
          "options": [
            { "name": "Page", "value": "page" },
            { "name": "Section", "value": "section" },
            { "name": "API Call", "value": "api" }
          ]
        },
        {
          "component": "text",
          "name": "link",
          "label": "Target (page path / section ID / API URL, depending on Navigation Type above)",
          "valueType": "string",
          "required": true
        },
        {
          "component": "text",
          "name": "linkText",
          "label": "Button Text",
          "valueType": "string",
          "required": true
        },
        {
          "component": "text",
          "name": "linkTitle",
          "label": "Title (tooltip)",
          "valueType": "string"
        },
        {
          "component": "select",
          "name": "linkType",
          "label": "Style",
          "valueType": "string",
          "options": [
            { "name": "Default", "value": "" },
            { "name": "Primary", "value": "primary" },
            { "name": "Secondary", "value": "secondary" }
          ]
        },
        {
          "component": "aem-content",
          "name": "apiSuccessPage",
          "label": "Navigate to (only used when Navigation Type = API Call, after a successful response)"
        }
      ]
    }
  ],
  "filters": [
    { "id": "nav-button", "components": [] }
  ]
}
